{
    "name": "wrapper",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/wrapper/src",
    "projectType": "application",
    "tags": [],
    "// targets": "to see all targets run: nx show project wrapper --web",
    "targets": {
        "serve": {
            "options": {
                "args": ["--port", "4200"]
            }
        }
    }
}
